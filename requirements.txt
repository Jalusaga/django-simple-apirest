# Django 4.2 LTS — last release that supports Python 3.8
Django==4.2.23

# DRF pinned to the last 3.x that has 3.8 wheels
djangorestframework==3.15.2

# ASGI support
asgiref==3.8.1

# C-FFI (needed by cryptography)
cffi==1.17.1

# Encryption lib — has manylinux cp37-abi3 wheels
cryptography==45.0.4

# Pure-Python parser for C-FFI
pycparser==2.22

# Pure-Python MySQL driver
PyMySQL==1.1.1

# Dot-env loader (1.0.1 is the highest for 3.8)
python-dotenv==1.0.1

# SQL formatter/parser
sqlparse==0.5.3

gunicorn==20.1.0
